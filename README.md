# rrd-athena-saml

A Scala SAML app (using the Play framework), built on top of a heroku project   

The code is deployed to heroku at https://damp-temple-15793.herokuapp.com/saml.  

A post request is required with parameter user and optional parameter requestID.

Response a SAML XML response.